name: Build Iris HDR Viewfinder Test V1.4.15

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'app/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - 'scripts/**'
      - '.github/workflows/build.yml'
      - 'BUILD_WORKFLOW_COPY.yml'

permissions:
  contents: read
  actions: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Checkout exact candidate history
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Download exact successful V1.4.14 Actions authority artifact
        uses: actions/download-artifact@v4
        with:
          name: Iris-HDR-Viewfinder-Test-V1.4.14
          path: ${{ runner.temp }}/v1414-authority-artifact
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: 33507939111

      - name: Reconstruct exact V1.4.14 authority and prove candidate before compilers
        shell: bash
        run: |
          set -euo pipefail
          authority='93c43a08e32eed5a0ccb146b81dd3907b2474b39'
          authority_tree='c0cf1f2f513a89ad883e3f349decff6e034fef5a'
          backup='backup-v1.4.14-pre-adaptive-hdr'
          backup_authority='93c43a08e32eed5a0ccb146b81dd3907b2474b39'
          authority_dir="$RUNNER_TEMP/v1414-authority-artifact"
          authority_src="$RUNNER_TEMP/v1414-authority-source"

          test "$GITHUB_SHA" = "$(git rev-parse HEAD)"
          test "$GITHUB_REF_NAME" = 'main'
          test "$(git rev-parse HEAD^)" = "$authority"
          test "$(git rev-parse "$authority^{tree}")" = "$authority_tree"
          backup_sha="$(git ls-remote origin "refs/heads/$backup" | awk '{print $1}')"
          test "$backup_sha" = "$backup_authority"
          cmp -s .github/workflows/build.yml BUILD_WORKFLOW_COPY.yml
          grep -Fq 'versionCode = 20' app/build.gradle.kts
          grep -Fq 'versionName = "1.0-v1.4.15"' app/build.gradle.kts
          grep -Fq 'compileSdk = 37' app/build.gradle.kts
          grep -Fq 'minSdk = 29' app/build.gradle.kts
          grep -Fq 'targetSdk = 37' app/build.gradle.kts
          grep -Fq 'version "9.2.1"' build.gradle.kts
          sha256sum -c PACKAGE_MANIFEST_SHA256.txt

          test "$(sha256sum "$authority_dir/Iris-HDR-Viewfinder-Test-V1.4.14-debug.apk" | awk '{print $1}')" = 'cbdf715c516767b1e806e055dcb317a645d2d5b8674a727c00963bf1404d8107'
          test "$(sha256sum "$authority_dir/Iris-HDR-Viewfinder-Test-V1.4.14-source-candidate.tar" | awk '{print $1}')" = 'b3752fa393c498ee1879e358f96f16705abf6fe8a21a9fb2cd14a8622712ffb2'
          test "$(sha256sum "$authority_dir/RUNTIME_SOURCE_MANIFEST.tsv" | awk '{print $1}')" = 'b8be504ae0047eb7124a25c2dcbda3e64a47be8d63e2c0c31863d6b555523b6b'
          test "$(sha256sum "$authority_dir/REPOSITORY_SOURCE_MANIFEST.tsv" | awk '{print $1}')" = '375a6ef7aef7ab108c235c5d58c757c4b68997bac9ae29bc0d35f11b2e421ede'
          test "$(sha256sum "$authority_dir/REGRESSIONS.txt" | awk '{print $1}')" = 'febf1c4a24c0e71ba6c6888384e1ce7e8158f0fcdda2e75132f678b1047c84ca'
          test "$(cat "$authority_dir/SOURCE_COMMIT.txt")" = "$authority"

          rm -rf "$authority_src"
          mkdir -p "$authority_src"
          tar -xf "$authority_dir/Iris-HDR-Viewfinder-Test-V1.4.14-source-candidate.tar" -C "$authority_src"

          python3 - "$authority_src" "$authority_dir" <<'PY'
          from pathlib import Path
          import hashlib, subprocess, sys, tempfile

          src = Path(sys.argv[1])
          artifact = Path(sys.argv[2])
          expected_runtime_sha = 'b8be504ae0047eb7124a25c2dcbda3e64a47be8d63e2c0c31863d6b555523b6b'
          expected_repo_sha = '375a6ef7aef7ab108c235c5d58c757c4b68997bac9ae29bc0d35f11b2e421ede'

          def row(p, rel):
              b = p.read_bytes()
              lines = b.count(b'\n') + (1 if b and not b.endswith(b'\n') else 0)
              return f"{hashlib.sha256(b).hexdigest()}\t{len(b)}\t{lines}\t{rel}\n"

          runtime_files = sorted(p for p in (src / 'app/src').rglob('*') if p.is_file())
          if len(runtime_files) != 12:
              raise SystemExit(f'V1.4.14 AUTHORITY RUNTIME COUNT FAIL: {len(runtime_files)}')
          runtime = ''.join(row(p, p.relative_to(src).as_posix()) for p in runtime_files).encode()
          if hashlib.sha256(runtime).hexdigest() != expected_runtime_sha:
              raise SystemExit('V1.4.14 AUTHORITY RUNTIME MANIFEST SHA FAIL')
          if runtime != (artifact / 'RUNTIME_SOURCE_MANIFEST.tsv').read_bytes():
              raise SystemExit('V1.4.14 AUTHORITY RUNTIME MANIFEST BYTE-EQUALITY FAIL')

          tracked = sorted(p for p in src.rglob('*') if p.is_file())
          if len(tracked) != 25:
              raise SystemExit(f'V1.4.14 AUTHORITY REPOSITORY COUNT FAIL: {len(tracked)}')
          repo = ''.join(row(p, p.relative_to(src).as_posix()) for p in tracked).encode()
          if hashlib.sha256(repo).hexdigest() != expected_repo_sha:
              raise SystemExit('V1.4.14 AUTHORITY REPOSITORY MANIFEST SHA FAIL')
          if repo != (artifact / 'REPOSITORY_SOURCE_MANIFEST.tsv').read_bytes():
              raise SystemExit('V1.4.14 AUTHORITY REPOSITORY MANIFEST BYTE-EQUALITY FAIL')

          with tempfile.TemporaryDirectory() as td:
              tar_path = Path(td) / 'authority.tar'
              subprocess.run(['git', 'archive', '--format=tar', f'--output={tar_path}', '93c43a08e32eed5a0ccb146b81dd3907b2474b39'], check=True)
              checkout = Path(td) / 'git-archive'
              checkout.mkdir()
              subprocess.run(['tar', '-xf', str(tar_path), '-C', str(checkout)], check=True)
              git_files = sorted(p for p in checkout.rglob('*') if p.is_file())
              if [p.relative_to(checkout).as_posix() for p in git_files] != [p.relative_to(src).as_posix() for p in tracked]:
                  raise SystemExit('V1.4.14 AUTHORITY ARTIFACT/GIT PATH SET FAIL')
              for gp, ap in zip(git_files, tracked):
                  if gp.read_bytes() != ap.read_bytes():
                      raise SystemExit(f'V1.4.14 AUTHORITY ARTIFACT/GIT BYTE FAIL: {gp.relative_to(checkout)}')
          print('EXACT V1.4.14 ACTIONS ARTIFACT AUTHORITY: PASS')
          PY

          bad_platform='platforms;android-37'
          if grep -Fq "sdkmanager \"$bad_platform\"" .github/workflows/build.yml; then
            echo 'REGRESSION FAIL 001: invalid API 37 package identifier survived' >&2
            exit 1
          fi
          if grep -RFn 'readAllBytes(' app/src/main/java; then
            echo 'REGRESSION FAIL 003: API-33 InputStream.readAllBytes survived minSdk 29 source' >&2
            exit 1
          fi
          if grep -RFn 'meta.timestampNs' app/src/main/java; then
            echo 'REGRESSION FAIL 015: failed V1.4 FrameMeta timestamp field reference returned' >&2
            exit 1
          fi

          python3 - <<'PY'
          from pathlib import Path
          import hashlib, subprocess

          authority = '93c43a08e32eed5a0ccb146b81dd3907b2474b39'
          expected_changes = {
              '.github/workflows/build.yml': 'M',
              'BUILD_WORKFLOW_COPY.yml': 'M',
              'PACKAGE_INFO.txt': 'M',
              'PACKAGE_MANIFEST_SHA256.txt': 'M',
              'README.md': 'M',
              'VSCODE_DEV_UPLOAD.txt': 'M',
              'app/build.gradle.kts': 'M',
              'app/src/main/assets/shaders/hdr_display.frag': 'M',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/CameraController.java': 'M',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/CaptureSetSaver.java': 'M',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/HdrGlView.java': 'M',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/JpegFusion.java': 'M',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/MainActivity.java': 'M',
              'scripts/verify_orientation.py': 'M',
          }
          raw = subprocess.check_output(['git', 'diff', '--name-status', authority, 'HEAD'], text=True).splitlines()
          actual = {}
          for line in raw:
              parts = line.split('\t')
              actual[parts[-1]] = parts[0][0]
          if actual != expected_changes:
              raise SystemExit(f'V1.4.15 ALLOWLIST FAIL: actual={actual} expected={expected_changes}')

          expected_tracked = [
              '.github/workflows/build.yml', '.gitignore', 'BUILD_WORKFLOW_COPY.yml',
              'PACKAGE_INFO.txt', 'PACKAGE_MANIFEST_SHA256.txt', 'README.md', 'VSCODE_DEV_UPLOAD.txt',
              'app/build.gradle.kts', 'app/src/main/AndroidManifest.xml',
              'app/src/main/assets/shaders/copy_2d.frag',
              'app/src/main/assets/shaders/fullscreen.vert',
              'app/src/main/assets/shaders/hdr_display.frag',
              'app/src/main/assets/shaders/oes_to_rgb.frag',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/CameraController.java',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/CaptureSetSaver.java',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/FrameMeta.java',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/HdrGlView.java',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/JpegFusion.java',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/MainActivity.java',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/MediaStoreWriter.java',
              'build.gradle.kts', 'gradle.properties', 'scripts/verify_orientation.py',
              'scripts/verify_shaders.py', 'settings.gradle.kts',
          ]
          tracked = subprocess.check_output(['git', 'ls-files'], text=True).splitlines()
          if tracked != expected_tracked:
              raise SystemExit(f'REGRESSION FAIL 002: tracked mismatch actual={tracked}')

          files = sorted(p for p in Path('app/src').rglob('*') if p.is_file())
          if len(files) != 12:
              raise SystemExit(f'V1.4.15 RUNTIME FILE COUNT FAIL: {len(files)}')
          rows=[]
          for p in files:
              b=p.read_bytes(); lines=b.count(b'\n') + (1 if b and not b.endswith(b'\n') else 0)
              rows.append(f"{hashlib.sha256(b).hexdigest()}\t{len(b)}\t{lines}\t{p.as_posix()}\n")
          manifest=''.join(rows).encode()
          digest=hashlib.sha256(manifest).hexdigest()
          expected='9edafd4c1a59369434a9d792022c7124b1633bf1b4e2230912cf13ab18e31db0'
          if digest != expected:
              raise SystemExit(f'V1.4.15 RUNTIME MANIFEST FAIL: {digest} != {expected}')
          Path('bootstrap-runtime-manifest.tsv').write_bytes(manifest)

          protected = {
              'app/src/main/AndroidManifest.xml': 'c183cb4caf4c9c8aa40a41d3891c197d9e84db196c6ecccd822b1c424ebc73e6',
              'app/src/main/assets/shaders/copy_2d.frag': 'b6b890e3be034a0d4980fd324fe0f469c7a2b0a126e958607bc8c4b9fbf97a55',
              'app/src/main/assets/shaders/fullscreen.vert': '7d8ee58c4500a46dd9614404a17ea4c7e104a6bf43976ecb85795799fdcd7fb4',
              'app/src/main/assets/shaders/oes_to_rgb.frag': '388d49929564efba24900794e035f88b461485f4da4f2e12caf71e3531ab6443',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/FrameMeta.java': 'e05484de966351424d3a5399eb159daccba766ed177c973b4b7529af014a60c5',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/MediaStoreWriter.java': 'a975fb65529c864a8bcefcded6c5df68fa881e7f97040734c6b0e326d27cc110',
          }
          for rel, sha in protected.items():
              got=hashlib.sha256(Path(rel).read_bytes()).hexdigest()
              if got != sha:
                  raise SystemExit(f'V1.4.15 PROTECTED RUNTIME FAIL: {rel} {got} != {sha}')
          print(f'V1.4.15 MINIMUM CANDIDATE PASS: changed={len(actual)} runtime=12 digest={digest} protected={len(protected)}/{len(protected)}')
          PY

          patch_sha=''
          rollback_sha=''
          for abbrev in 7 12 40; do
            git -c core.abbrev="$abbrev" diff --binary --full-index "$authority" HEAD > "$RUNNER_TEMP/forward-$abbrev.patch"
            git -c core.abbrev="$abbrev" diff --binary --full-index HEAD "$authority" > "$RUNNER_TEMP/rollback-$abbrev.patch"
            if test -z "$patch_sha"; then
              patch_sha="$(sha256sum "$RUNNER_TEMP/forward-$abbrev.patch" | awk '{print $1}')"
              rollback_sha="$(sha256sum "$RUNNER_TEMP/rollback-$abbrev.patch" | awk '{print $1}')"
            else
              test "$(sha256sum "$RUNNER_TEMP/forward-$abbrev.patch" | awk '{print $1}')" = "$patch_sha"
              test "$(sha256sum "$RUNNER_TEMP/rollback-$abbrev.patch" | awk '{print $1}')" = "$rollback_sha"
            fi
          done
          cmp -s "$RUNNER_TEMP/forward-7.patch" "$RUNNER_TEMP/forward-12.patch"
          cmp -s "$RUNNER_TEMP/forward-7.patch" "$RUNNER_TEMP/forward-40.patch"
          cmp -s "$RUNNER_TEMP/rollback-7.patch" "$RUNNER_TEMP/rollback-12.patch"
          cmp -s "$RUNNER_TEMP/rollback-7.patch" "$RUNNER_TEMP/rollback-40.patch"
          cp "$RUNNER_TEMP/forward-7.patch" "$RUNNER_TEMP/V1.4.14_to_V1.4.15.forward.patch"
          cp "$RUNNER_TEMP/rollback-7.patch" "$RUNNER_TEMP/V1.4.15_to_V1.4.14.rollback.patch"

          python3 - "$authority_src" "$GITHUB_WORKSPACE" "$RUNNER_TEMP/V1.4.14_to_V1.4.15.forward.patch" "$RUNNER_TEMP/V1.4.15_to_V1.4.14.rollback.patch" <<'PY'
          from pathlib import Path
          import shutil, subprocess, sys, tempfile
          authority, candidate, forward, rollback = map(Path, sys.argv[1:])
          tracked = subprocess.check_output(['git', '-C', str(candidate), 'ls-files'], text=True).splitlines()
          def compare(left, right, label):
              for rel in tracked:
                  a=(left/rel).read_bytes(); b=(right/rel).read_bytes()
                  if a != b: raise SystemExit(f'{label} BYTE FAIL: {rel}')
          with tempfile.TemporaryDirectory() as td:
              t=Path(td)
              g=t/'git-apply'; shutil.copytree(authority, g)
              subprocess.run(['git','apply',str(forward)],cwd=g,check=True)
              compare(g,candidate,'GIT FORWARD')
              subprocess.run(['git','apply',str(rollback)],cwd=g,check=True)
              compare(g,authority,'GIT ROLLBACK')
              p=t/'gnu-patch'; shutil.copytree(authority,p)
              subprocess.run(['patch','-p1','--fuzz=0','-i',str(forward)],cwd=p,check=True)
              compare(p,candidate,'GNU FUZZ0 FORWARD')
              subprocess.run(['patch','-p1','--fuzz=0','-i',str(rollback)],cwd=p,check=True)
              compare(p,authority,'GNU FUZZ0 ROLLBACK')
          print('FORWARD/ROLLBACK PATCH REPLAY: PASS')
          PY
          printf '%s\n' "$patch_sha" > "$RUNNER_TEMP/FORWARD_PATCH_SHA256.txt"
          printf '%s\n' "$rollback_sha" > "$RUNNER_TEMP/ROLLBACK_PATCH_SHA256.txt"

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install and prove Android 37 SDK
        shell: bash
        run: |
          set -euo pipefail
          sdkmanager --list > "$RUNNER_TEMP/sdkmanager-list.txt"
          grep -Fq 'platforms;android-37.0' "$RUNNER_TEMP/sdkmanager-list.txt"
          sdkmanager "platforms;android-37.0"
          test -f "$ANDROID_SDK_ROOT/platforms/android-37.0/android.jar"

      - name: Set up Gradle 9.6.0
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.0'

      - name: Prepare stable V1.4.15 test signing identity
        shell: bash
        env:
          IRIS_TEST_SIGNING_KEY_B64: ${{ secrets.IRIS_TEST_SIGNING_KEY_B64 }}
        run: |
          set -euo pipefail
          test -n "${IRIS_TEST_SIGNING_KEY_B64:-}" || { echo 'Missing required Actions secret IRIS_TEST_SIGNING_KEY_B64' >&2; exit 1; }
          key="$RUNNER_TEMP/iris-hdr-test-signing.p12"
          printf '%s' "$IRIS_TEST_SIGNING_KEY_B64" | base64 -d > "$key"
          test "$(sha256sum "$key" | awk '{print $1}')" = '2a4ec2ab3fed7ae4d2e9c1b6b80c3b5bb19f07420952e97c203fda31e69cff2e'
          fingerprint="$(keytool -list -v -storetype PKCS12 -keystore "$key" -storepass IrisHdrTest2026 -alias iris-hdr-test | awk -F'SHA256: ' '/SHA256:/{gsub(/^[ \t]+|[ \t]+$/,"",$2); print $2; exit}')"
          test "$fingerprint" = '53:1A:EE:D9:EA:D7:9D:28:C4:24:AD:8F:71:A4:59:B4:CE:D8:AF:F3:7E:95:C1:1B:D2:95:08:3F:BB:25:C4:E8'
          echo "IRIS_TEST_KEYSTORE_PATH=$key" >> "$GITHUB_ENV"
          printf '%s\n' '2a4ec2ab3fed7ae4d2e9c1b6b80c3b5bb19f07420952e97c203fda31e69cff2e' > "$RUNNER_TEMP/SIGNING_KEY_SHA256.txt"
          printf '%s\n' "$fingerprint" > "$RUNNER_TEMP/SIGNING_CERT_SHA256.txt"

      - name: Install pinned real GLSL compiler
        shell: bash
        run: |
          set -euo pipefail
          expected='15.1.0-2~ubuntu0.24.04.2'
          sudo apt-get update
          sudo apt-get install -y "glslang-tools=$expected"
          test "$(dpkg-query -W -f='${Version}' glslang-tools)" = "$expected"
          glslangValidator --version

      - name: Scan complete GLSL reserved identifiers
        shell: bash
        run: python3 scripts/verify_shaders.py

      - name: Compile exact runtime GLSL assets
        shell: bash
        run: |
          set -euo pipefail
          glslangValidator -l app/src/main/assets/shaders/fullscreen.vert app/src/main/assets/shaders/oes_to_rgb.frag
          glslangValidator -l app/src/main/assets/shaders/fullscreen.vert app/src/main/assets/shaders/copy_2d.frag
          glslangValidator -l app/src/main/assets/shaders/fullscreen.vert app/src/main/assets/shaders/hdr_display.frag

      - name: Compile modified Java with real project compiler
        shell: bash
        run: gradle --no-daemon --stacktrace :app:compileDebugJavaWithJavac

      - name: Replay V1.4.15 runtime regressions on compiler-tested candidate
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/verify_orientation.py
          python3 scripts/verify_shaders.py

      - name: Build full debug APK from frozen compiler-tested candidate
        shell: bash
        run: gradle --no-daemon --stacktrace :app:assembleDebug

      - name: Post-build invariance and freeze compiled V1.4.15 candidate
        shell: bash
        run: |
          set -euo pipefail
          git diff --exit-code -- .
          python3 scripts/verify_orientation.py
          python3 scripts/verify_shaders.py
          sha256sum -c PACKAGE_MANIFEST_SHA256.txt
          cmp -s .github/workflows/build.yml BUILD_WORKFLOW_COPY.yml

          python3 - <<'PY'
          from pathlib import Path
          import hashlib, subprocess
          expected_runtime='9edafd4c1a59369434a9d792022c7124b1633bf1b4e2230912cf13ab18e31db0'
          files=sorted(p for p in Path('app/src').rglob('*') if p.is_file())
          rows=[]
          for p in files:
              b=p.read_bytes(); lines=b.count(b'\n') + (1 if b and not b.endswith(b'\n') else 0)
              rows.append(f"{hashlib.sha256(b).hexdigest()}\t{len(b)}\t{lines}\t{p.as_posix()}\n")
          manifest=''.join(rows).encode(); digest=hashlib.sha256(manifest).hexdigest()
          if len(files)!=12 or digest!=expected_runtime:
              raise SystemExit(f'POST-BUILD RUNTIME INVARIANCE FAIL files={len(files)} digest={digest}')
          Path('compiled-runtime-manifest.tsv').write_bytes(manifest)
          tracked=subprocess.check_output(['git','ls-files'],text=True).splitlines()
          if len(tracked) != 25:
              raise SystemExit(f'POST-BUILD TRACKED COUNT FAIL: {len(tracked)}')
          repo=[]
          for rel in tracked:
              b=Path(rel).read_bytes(); lines=b.count(b'\n') + (1 if b and not b.endswith(b'\n') else 0)
              repo.append(f"{hashlib.sha256(b).hexdigest()}\t{len(b)}\t{lines}\t{rel}\n")
          Path('compiled-repository-manifest.tsv').write_text(''.join(repo))
          protected = {
              'app/src/main/AndroidManifest.xml': 'c183cb4caf4c9c8aa40a41d3891c197d9e84db196c6ecccd822b1c424ebc73e6',
              'app/src/main/assets/shaders/copy_2d.frag': 'b6b890e3be034a0d4980fd324fe0f469c7a2b0a126e958607bc8c4b9fbf97a55',
              'app/src/main/assets/shaders/fullscreen.vert': '7d8ee58c4500a46dd9614404a17ea4c7e104a6bf43976ecb85795799fdcd7fb4',
              'app/src/main/assets/shaders/oes_to_rgb.frag': '388d49929564efba24900794e035f88b461485f4da4f2e12caf71e3531ab6443',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/FrameMeta.java': 'e05484de966351424d3a5399eb159daccba766ed177c973b4b7529af014a60c5',
              'app/src/main/java/com/skyking0007/irishdrviewfinder/MediaStoreWriter.java': 'a975fb65529c864a8bcefcded6c5df68fa881e7f97040734c6b0e326d27cc110',
          }
          for rel, expected in protected.items():
              got=hashlib.sha256(Path(rel).read_bytes()).hexdigest()
              if got != expected: raise SystemExit(f'POST-BUILD PROTECTED FAIL: {rel}')
          print('POST-BUILD FROZEN-CANDIDATE/PROTECTED INVARIANCE: PASS')
          PY

          mapfile -t apks < <(find app/build/outputs/apk/debug -maxdepth 1 -type f -name '*.apk' -print | sort)
          test ${#apks[@]} -eq 1
          apksigner="$(find "$ANDROID_SDK_ROOT/build-tools" -type f -name apksigner -print | sort -V | tail -n 1)"
          test -n "$apksigner"
          "$apksigner" verify --print-certs "${apks[0]}" > "$RUNNER_TEMP/apk-signing-cert.txt"
          grep -Fiq '531aeed9ead79d28c424ad8f71a459b4ced8aff37e95c11bd295083fbb25c4e8' "$RUNNER_TEMP/apk-signing-cert.txt"
          mkdir -p output
          printf '%s\n' '2a4ec2ab3fed7ae4d2e9c1b6b80c3b5bb19f07420952e97c203fda31e69cff2e' > output/SIGNING_KEYSTORE_SHA256.txt
          printf '%s\n' '531aeed9ead79d28c424ad8f71a459b4ced8aff37e95c11bd295083fbb25c4e8' > output/SIGNING_CERT_SHA256.txt
          cp "${apks[0]}" output/Iris-HDR-Viewfinder-Test-V1.4.15-debug.apk
          git archive --format=tar --output=output/Iris-HDR-Viewfinder-Test-V1.4.15-source-candidate.tar "$GITHUB_SHA"
          printf '%s\n' "$GITHUB_SHA" > output/SOURCE_COMMIT.txt
          cp compiled-runtime-manifest.tsv output/RUNTIME_SOURCE_MANIFEST.tsv
          cp compiled-repository-manifest.tsv output/REPOSITORY_SOURCE_MANIFEST.tsv
          cp "$RUNNER_TEMP/V1.4.14_to_V1.4.15.forward.patch" output/V1.4.14_to_V1.4.15.forward.patch
          cp "$RUNNER_TEMP/V1.4.15_to_V1.4.14.rollback.patch" output/V1.4.15_to_V1.4.14.rollback.patch
          cp "$RUNNER_TEMP/FORWARD_PATCH_SHA256.txt" output/FORWARD_PATCH_SHA256.txt
          cp "$RUNNER_TEMP/ROLLBACK_PATCH_SHA256.txt" output/ROLLBACK_PATCH_SHA256.txt
          cat > output/REGRESSIONS.txt <<'REGRESSIONS'
          001 - API 37 sdkmanager package must be platforms;android-37.0, never platforms;android-37.
          002 - vscode.dev upload must preserve the complete 25-file tracked candidate and all 12 app/src runtime files.
          003 - minSdk 29 runtime source must not call InputStream.readAllBytes(), which is Android API 33+.
          004 - UI must follow fullUser orientation and respect Android orientation lock in portrait and landscape.
          005 - SurfaceTexture transform matrix owns producer-origin/crop correction exactly once; retired manual YUV flip must not coexist.
          006 - NORMAL/HDR/SPLIT use FIT presentation with no stretch and no center-crop-to-fill.
          007 - SHORT/LONG JPEG orientation follows device orientation and fused JPEG normalizes EXIF-only HAL rotation.
          008 - RAW/native sensor aspect drives PRIVATE preview and JPEG selection; forced 16:9 scoring must never return.
          009 - Live preview remains direct PRIVATE SurfaceTexture/external-OES GPU input; per-frame Java YUV repacking stays removed.
          010 - Cadence policy stays explicit: FOV SAFE fixed 30 fps; 60 FPS CROP ON is the only exact [60,60] path.
          011 - Camera2 processed output uses explicit TONEMAP_MODE_CONTRAST_CURVE + sampled sRGB TONEMAP_CURVE when supported.
          012 - Clean AE is initial bootstrap only; once one anchor exists, no periodic AE repeating-request takeover may interrupt live HDR.
          013 - AUTO LONG owns desired scene appearance while SHORT independently owns adaptive highlight headroom; V1.4.14 whole-pair coupling must not return.
          014 - Upload/source candidate contains no temporary Python __pycache__ or .pyc outputs.
          015 - HdrGlView timestamp matcher uses FrameMeta.sensorTimestampNs; failed meta.timestampNs reference must never return.
          016 - Rejected V1.4.2 sideways preview: SurfaceTexture producer transform is consumed exactly once with no second display quarter-turn.
          017 - HDR/SPLIT publish only complete temporally adjacent SHORT/LONG pairs; no half-updated fusion.
          018 - AUTO HDR and HDR MANUAL remain available; MANUAL keeps user LONG/brightness intent but retains adaptive SHORT intelligence.
          019 - Known 50/60-Hz timing remains explicit; unknown/PWM never invents unsafe LONG timing.
          020 - CaptureResult FPS/frame duration remains diagnostic only; explicit cropped-60 under-delivery never silently mutates target cadence.
          021 - V1.4.2/V1.4.14 meter-lag regression: no one-shot capture() meter and no recurring 5-second setRepeatingRequest meter takeover after bootstrap.
          022 - Portrait NORMAL/SPLIT/HDR share one live orientation owner; sensor/display relation changes FIT axes only.
          023 - Every DNG explicitly sets a valid TIFF orientation 1/6/3/8 before writeImage; Orientation=9 must never return.
          024 - CaptureResult FPS measurement window starts on the first delivered result, never camera-open/session startup time.
          025 - Both steady HDR members carry the selected CONTROL_AE_TARGET_FPS_RANGE; forced true-60 uses exact [60,60].
          026 - V1.4.3/V1.4.5 FOV transition regression: FOV SAFE starts/stays 30; cropped 60 is explicit and never constrains still RAW/JPEG.
          027 - MANUAL SAFE adapts SHORT headroom from scene clipping while never making SHORT longer than the user-requested ceiling.
          028 - Production diagnostics persist low-duty session/camera/FOV/flicker/capture/GPU/crash evidence without per-frame logging.
          029 - V1.4.4 dark AUTO regression: absolute scene reference comes from clean bootstrap AE, then continuous displayed LONG statistics adapt exposure without AE request oscillation.
          030 - LONG remains complete shadow/midtone/color owner wherever its data is valid; SHORT cannot globally alter ordinary scene RGB.
          031 - Manual exposure controls retain through 1/8000s, 1/100, 1/50, 30ms, 1/25 and 1/20 plus adaptive highlight headroom.
          032 - Clean-AE post-RAW sensitivity boost parity and explicit sRGB contrast-curve semantics remain cross-device requirements.
          033 - NOISE_REDUCTION_MODE_OFF and EDGE_MODE_OFF are requested wherever supported.
          034 - Hidden bootstrap METER frames never become HDR pair members or FOV/FPS evidence; after bootstrap METER frames cease entirely.
          035 - CaptureResult cadence windows reset around the one initial bootstrap only; there is no recurring meter cadence window.
          036 - V1.4.7 red/orange defect regression: one bright LONG channel must never trigger a full-RGB SHORT handoff.
          037 - V1.4.8/V1.4.10 aperture-driven bracket widening and global appearance/color lift remain rejected.
          038 - V1.4.15 authority is exact successful V1.4.14 run 33507939111 / commit 93c43a08e32eed5a0ccb146b81dd3907b2474b39.
          039 - Skin/reflective-color safety: saturated single chroma channel alone is insufficient; SHORT recovery also requires bright-luma or second-channel highlight evidence.
          040 - Shutter press freezes the current adaptive SHORT/LONG exposure/ISO pair, post-RAW boost and requested Brightness before still-session configuration.
          041 - Full-resolution fusion contains no post-fusion Brightness gain and introduces no per-pixel float-array allocation/exp/sqrt.
          042 - No neighborhood chroma smoothing, textureOffset/texelFetch cross-edge blur, or broad global color repair may enter adaptive HDR.
          043 - V1.4.11 failed post-fusion Brightness regression: live GLSL and saved JpegFusion remain unaware of Brightness EV.
          044 - V1.4.14 whole-pair Brightness regression: Brightness changes LONG appearance target; SHORT may move independently darker to preserve highlights.
          045 - Anti-banding guard: 50Hz uses 10ms periods, 60Hz uses 8.333333ms; SHORT may go below one period only when real highlight headroom requires it.
          046 - V1.4.12/V1.4.13 widening artifact regression: bracket may widen only from measured meaningful clipping, not as a direct function of Brightness EV.
          047 - Plant/bright-wall/window recovery uses coarse meaningful-clipped-area evidence plus SHORT quality/overlap gates; tiny speculars may intentionally clip.
          048 - Brightness slider stays -5.0..+2.0 EV in 0.1 steps and remains active in AUTO and MANUAL SAFE.
          049 - Compact controls reserve Android navigation/gesture inset so Brightness stays touchable above the system pill.
          050 - Status/debug text stays invariant one-line 20dp geometry; text changes can never resize the weighted viewfinder.
          051 - V1.4.14 scene-detection lag becomes permanent regression: live statistics are 32x24 at 200ms cadence and must be wired HdrGlView -> CameraController.
          052 - 95%-HDR policy: meaningful LONG clipping begins at 0.5% sampled area; AUTO SHORT target is 0.25%, MANUAL target 0.15%, allowing tiny residual clipping.
          053 - SHORT is exposure-normalized then overlap-calibrated per channel; only genuinely lost LONG channels may consume calibrated SHORT.
          054 - MANUAL uses the same adaptive bracket engine as AUTO with 0.25EV extra headroom and up to 6EV total headroom, subject to quality gates.
          055 - AUTO adaptive LONG changes are bounded to 0.30EV per live update; continuous sensing must not become abrupt multi-stop jumps.
          056 - Stable update signing uses only GitHub secret IRIS_TEST_SIGNING_KEY_B64 + runner-temp PKCS12; no private signing key is tracked in the repository.
          057 - Stable signing keystore SHA-256 is 2a4ec2ab3fed7ae4d2e9c1b6b80c3b5bb19f07420952e97c203fda31e69cff2e.
          058 - Every V1.4.15+ APK must verify to signing certificate SHA-256 531aeed9ead79d28c424ad8f71a459b4ced8aff37e95c11bd295083fbb25c4e8 before artifact export.
          REGRESSIONS
          sha256sum output/Iris-HDR-Viewfinder-Test-V1.4.15-debug.apk \
                    output/Iris-HDR-Viewfinder-Test-V1.4.15-source-candidate.tar \
                    output/RUNTIME_SOURCE_MANIFEST.tsv \
                    output/REPOSITORY_SOURCE_MANIFEST.tsv \
                    output/V1.4.14_to_V1.4.15.forward.patch \
                    output/V1.4.15_to_V1.4.14.rollback.patch \
                    output/REGRESSIONS.txt \
                    output/SIGNING_KEYSTORE_SHA256.txt \
                    output/SIGNING_CERT_SHA256.txt | tee output/SHA256SUMS.txt

      - name: Upload compiled V1.4.15 candidate artifact
        uses: actions/upload-artifact@v4
        with:
          name: Iris-HDR-Viewfinder-Test-V1.4.15
          path: output/
          if-no-files-found: error
          retention-days: 14
